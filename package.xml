<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>gz_ros2_bridge_manager</name>
  <version>0.1.0</version>
  <description>
    Gazebo Harmonic GUI plugin: discover Gazebo Transport topics from running
    worlds and generate ros_gz_bridge parameter_bridge commands to expose
    those topics to ROS 2 Jazzy. Works with any models already present in
    the simulation, without requiring URDF/XACRO/SDF source files.
  </description>
  <maintainer email="asoriano@robotnik.es">Robotnik</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Gazebo Harmonic (Jazzy vendor packages) -->
  <depend>gz_gui_vendor</depend>
  <depend>gz_transport_vendor</depend>
  <depend>gz_msgs_vendor</depend>

  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
